#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <conio.h>
#include <math.h>

using namespace std;
float suma( float n,float a);

void main()
{float s,a;
	float x,n;
	do{
	cout<<"ingrese n ";
cin>> n;
}while(x<=0);
do{
cout<<"ingrese  x ";
cin>> x;
}while(x<=0);
a=(1+(1/x));

s=suma(n,a);
cout<<"el resultado es"<<s;
getch();
}

float suma( float n,float a)
{int i;
float s=0;
for(i=1;i<=n;i++)
{
s=s+pow(a,i);

}
return(s);
}
